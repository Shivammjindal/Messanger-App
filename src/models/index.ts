import mongoose from "mongoose";

export const User = mongoose.models.users
export const Message = mongoose.models.messages
export const Conversation = mongoose.models.conversations
export const Account = mongoose.models.messages